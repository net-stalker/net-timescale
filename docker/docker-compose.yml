version: '3.8'

services:
  timescaledb:
    build:
      context: timescaledb
    hostname: timescaledb
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_PASSWORD=PsWDgxZb
    networks:
      - skynet
networks:
  skynet:
    driver: bridge